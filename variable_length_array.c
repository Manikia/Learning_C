#include <stdio.h>

int main(void)
{
        // size_t size = 0;
        // printf("Enter # of elements you want to store");
        // scanf("%zd", &size);
        // //%zd is an unsigned integer, means size d
        
        // //we are creating a float array w the size of size
        // float values[size];

        // //if we want to expand we do
        // size_t rows = 0;
        // size_t columns = 0;


        // printf("Enter # of rows you want to store ");
        // scanf("%zd", &rows);
        // //we do zd bc its a long unsigned #

        // printf("Enter # of columns you want to store ");
        // scanf("%zd", &columns);
        // //%zd is an unsigned integer, means size d

        // //we are creating a float array w row and columns so a 2D array
        // float beans[rows][columns];

        // return 0;

}