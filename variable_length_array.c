#include <stdio.h>

int main(){
        size_t size = 0;
        printf("Enter # of elements you want to store");
        scanf("%zd", &size);
        //%zd is an unsigned integer

        float values[size];
}