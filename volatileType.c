//tells variable that it will change its value
//restrict is an optimixzationhint for compiler
//without restriction it asssumes worst case

#include <stdio.h>

int main()
{
    volatile int loc1; //volatile value
    //goto someLabel; //goesto the statement that has the label of 'somelabel'
    //goto isnt very liked
    //ex:
    //goto jump
    //jump:
        //statement with what i want it to do
    //the above work will go from the goto to jump since thats
    //where im telling it to goto**
    return 0;
}